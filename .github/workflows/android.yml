name: Build Android APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.11.1'
      - name: Apply staged build 108 source
        run: |
          if [ -f tools/MainActivity_108.kt.gz.b64 ]; then
            base64 -d tools/MainActivity_108.kt.gz.b64 | gzip -dc > app/src/main/java/com/brannenservices/fieldassistant/MainActivity.kt
          fi
      - name: Build debug APK
        run: gradle assembleDebug
      - uses: actions/upload-artifact@v4
        with:
          name: field-assistant-debug
          path: app/build/outputs/apk/debug/app-debug.apk
      - name: Bake corrected source into main
        if: success()
        run: |
          if [ -f tools/MainActivity_108.kt.gz.b64 ]; then
            rm tools/MainActivity_108.kt.gz.b64
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git add app/src/main/java/com/brannenservices/fieldassistant/MainActivity.kt tools/MainActivity_108.kt.gz.b64
            git commit -m "Bake build 108 corrections [skip ci]"
            git push origin HEAD:main
          fi
